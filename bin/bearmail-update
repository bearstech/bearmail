#!/usr/bin/perl
use lib '../lib';
use BearMail::Backend::Files;


my $b = BearMail::Backend::Files->new( debug => 0, mailmap => '/etc/mailmap' ); # Parse 'mailmap'
$b->apply();                             # Generate Postfix+Dovecot conf files and reload them
